services:
    app:
        build:
            args:
                # Não altere o nome de usuário abaixo
                user: dime
                uid: 1000
            context: ./
            dockerfile: docker/php/php.dockerfile
        image: php85
        extra_hosts:
            - host.docker.internal:host-gateway
        container_name: app
        restart: unless-stopped
        working_dir: /var/www
        volumes:
            - ./:/var/www
            - ./docker/php/xdebug.ini:/usr/local/etc/php/conf.d/docker-php-ext-xdebug.ini
            - ./docker/php/php.ini:/usr/local/etc/php/conf.d/php.ini
            - ./docker/php/.bashrc:/home/dime/.bashrc
        networks:
            - dev

    nginx:
        image: nginx:alpine
        container_name: nginx
        restart: unless-stopped
        ports:
            - "8000:8000"
        volumes:
            - ./:/var/www
            - ./docker/nginx:/etc/nginx/conf.d
        networks:
            - dev
        links:
            - app

    ############################
    # MySQL
    ############################
#    db:
#        image: mysql:latest
#        container_name: ${APP_NAME:-mysql}-db
#        restart: unless-stopped
#        environment:
#            MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-123456789}
#            MYSQL_DATABASE: ${DB_DATABASE:-skeleton}
#        ports:
#            - "${DB_PORT:-3306}:${DB_PORT:-3306}"
#        volumes:
#            - /home/${USUARIO:-dime}/mysqldb:/var/lib/mysql
#        networks:
#            - dev

    ############################
    # Postgres
    ############################
    db:
        image: postgres:16-alpine
        restart: unless-stopped
        container_name: db
        environment:
            POSTGRES_USER: postgres
            POSTGRES_PASSWORD: admin123
            POSTGRES_DB: meu_banco
        ports:
            - "5432:5432"
        volumes:
            - arquivos_postgres:/var/lib/postgresql/data
        networks:
            - dev

#    pgadmin:
#        image: dpage/pgadmin4:latest
#        container_name: pgadmin
#        restart: unless-stopped
#        environment:
#            PGADMIN_DEFAULT_EMAIL: admin@admin.com  # usuário de login
#            PGADMIN_DEFAULT_PASSWORD: admin         # senha de login
#        ports:
#            - "5050:80"  # acessa via http://localhost:5050
#        volumes:
#            - pgadmin_data:/var/lib/pgadmin  # persiste configurações
#        networks:
#            - dev
#        links:
#            - db

volumes:
    arquivos_postgres:
#    pgadmin_data:

networks:
    dev:
        driver: bridge
